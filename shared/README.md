## shared

This folder contains **cross-layer TypeScript definitions**.

- Shared types, enums, constants, and schemas used by `frontend/`, `backend/`, and `ai-workflow/`.
- Defines API request/response contracts so all layers agree on payload shapes.
- Should remain lightweight and framework-agnostic.

