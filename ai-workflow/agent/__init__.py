"""
agent/
The ReAct tutoring agent — bare-bones foundation.

Modules:
  - tools.py        : Tool registry (functions the agent can call)
  - react_loop.py   : The core think → act → observe loop
  - agent_prompt.py  : System prompt that makes the LLM an agent
"""
