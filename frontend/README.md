## frontend

This folder contains the **student-facing app** (mobile and/or web).

- Responsible for UI, navigation, local state, and calling backend APIs.
- Must **not** contain tutoring reasoning, prompt logic, or AI orchestration.
- Interacts with the backend only via typed API clients defined in `frontend/services` and contracts in `shared/`.

